---
title: "Day 9 — Minimal dashboard + deploy"
description: "A product-only dashboard (upload, status, results, trace link) deployed to a hosted URL, seeded with two tenants, kept private and shareable on request."
day_number: 9
status: upcoming
goal: "Minimal dashboard + deploy to a hosted URL, seed two tenants (dev docs), kept private/shareable."
non_negotiable: "Deployed and reachable"
---

<div class="hlog-nonneg">
  <b>Non-negotiable.</b> The product must actually be deployed and reachable at a real URL, not just runnable locally.
</div>

## The goal

Ship the minimal dashboard (upload, status, results, a trace link — product-only, no custom eval-visualization UI) and deploy the whole service to a free hosted URL, seeded with two tenants of developer-docs content. The deployed URL stays private, shareable to a recruiter only on direct request — it is never linked from this build-log site.

This day has not started yet. Once it lands, this page will be rewritten in place with the real story, proof, and architecture snapshot, sourced from the project's build log.
